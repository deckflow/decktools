version: 2

project_name: decktools

builds:
  - id: decktools
    dir: apps/go-cli
    main: .
    binary: decktools
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X=main.version={{ .Env.RELEASE_VERSION }}

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Env.RELEASE_VERSION }}_{{ .Os }}_{{ .Arch }}"
    files:
      - apps/go-cli/README.md

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

release:
  github:
    owner: deckflow
    name: decktools
  name_template: "go-cli v{{ .Env.RELEASE_VERSION }}"
  extra_files:
    - glob: ./scripts/decktools-installer.sh
